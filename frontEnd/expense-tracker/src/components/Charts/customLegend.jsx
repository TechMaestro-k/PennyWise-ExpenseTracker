import React from 'react'

const customLegend = ({payload}) => {
  return (
    <div className=''>
        {payload.map((entry,index)=>{
            <div className=''>
                
            </div>
        })}
    </div>
  )
}

export default customLegend